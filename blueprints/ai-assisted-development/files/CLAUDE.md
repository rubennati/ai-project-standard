<!-- blueprint payload: ai-assisted-development — edit upstream, then delete -->
# Claude Code

A pointer. The source of truth for every AI tool here is `AGENTS.md` at the
repository root.

Start with `.ai/project-brief.md`, `.ai/state.md`, `.ai/routing.md`.

The "Stop and ask", "Never" and "Approval" sections of `AGENTS.md` are binding.
Do not restate rules in this file; add them to `AGENTS.md`.
